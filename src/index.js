import React from 'react';
import ReactDOM from 'react-dom/client';
// Baris di bawah ini telah dijadikan komen atau dipadamkan
// import './index.css';
import App from './App'; // Mengimport komponen utama App dari App.js

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
